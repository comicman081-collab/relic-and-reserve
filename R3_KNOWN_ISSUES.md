# R3 Known Issues / Unverified Items

1. **Windows release held:** no R3 EXE/PCK or archive exists. Export, executable launch, Korean rendering inside the EXE, and executable full-campaign acceptance are UNVERIFIED by explicit user instruction.
2. **Not declared complete:** automated/source QA PASS is not a product-completion declaration. Further design, writing, art, accessibility, and human playtest work may still be requested.
3. **Duration is estimated:** no timed human 8–12 hour or 15–25+ hour playtest was conducted.
4. **Rights review remains open:** the legacy manifest has 238 catalogued assets: the Victor Hale, Lena Falk, Hana Mire, Mara Venn and Iris Bell portrait rows plus the repo-native sextant are the six explicit `PROJECT_GENERATED` rows; the other 232 rows remain `RIGHTS_REVIEW_REQUIRED / UNVERIFIED`. No CC0 claim is made. Thirty-four newer `assets/ui` case/flow SVGs are still absent from this legacy manifest and remain dedicated completeness/provenance debt.
5. **Localization scope:** 106 paired EN/KO keys cover the core UI, campaign shell, ending, and postgame, and the sixteen authored-v2 dossiers carry their own paired copy. Proper names, much of the artifact catalog, the ten projected legacy cases, registry-anchor document bodies, and bidder/NPC names still need a final bilingual copy pass.
6. **Presentation scope:** the workshop and Grand Reserve are functional stylized 3D environments; final art polish and human UX/accessibility testing are not claimed. Material portrait changes require a fresh hidden/headless Godot import-raster pass followed by actual 96×120 and contact-sheet review so stale imported textures cannot satisfy the source-only gate.
7. **Authored migration remains open:** 16 of 26 campaign cases have authored-v2 evidence graphs. The remaining ten cases are playable projections, not finished investigations. A canonical local audit identifies two cases in the now-active Stage 7 tranche, but their IDs are not yet frozen and must not be invented ahead of review.
8. **Evidence weights remain quantized:** authored link weights are still converted to the established integer-strength contract (`>= 0.75 -> 2`, otherwise `1`). A separate design/economy audit is required before changing that mapping.
9. **Pre-isolation profile incident:** an earlier Stage 10 fixture wrote its completed-test profile into the shared Godot `user://` location before test isolation was added. The current profile and `.bak` both contain the later test state, so the prior profile cannot be recovered from those two files. Current runners use isolated APPDATA/LOCALAPPDATA roots and no longer write to the shared profile.
10. **Public DTO internal IDs remain open:** the player-safe metadata bridge sanitizes and times the rendered dossier copy, but public-state DTOs still retain internal evidence/routing IDs needed by generic actions. The bridge and current 70-view actual-framebuffer review confirm those tokens are not rendered on the covered surfaces; the artifact RenderDTO and pair-keyed instance overrides are whitelist-only, but a global DTO-redaction project has not been approved.
11. **Shadow Camera public provenance is unresolved:** the locked authored case has no public `PROVENANCE` bridge, so a strong report can still yield `PROVENANCE_UNCERTAIN` at listing. Fresh-only provenance and legacy `knownClues` backfill are explicitly deferred because either would change historical public state.
12. **Stage 5 registry anchor metadata is stale:** `document_25.usedByCase=master_camera` and `document_27.usedByCase=prologue_clock` do not name their new authored-v2 consumers. Runtime source binding is explicit and correct, but a later registry hygiene pass should reconcile these metadata anchors without rewriting campaign save authority.
13. **Stage 5 risky routes are optional at mastery:** each new case can reach four-source masterful using only safe sources, so the LOW repair-trace clue is a meaningful optional corroboration rather than a mandatory trade-off. Human playtesting must determine whether this is sufficiently visible and tempting.
14. **Grand Reserve visual-signature type debt remains contained:** the legacy `visual_signature` API returns `String` while one Grand Reserve snapshot expectation is dictionary-shaped. Automated containment proves the new render DTO does not alter that schema, but unification remains a separate compatibility task.
